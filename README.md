# EmployeeManagementService

### A full Stack Application where  we can perform all the CRUD operations over the employee. 

  ###  API's : 
        employees -> POST
        employees -> GET
        employees/{id} -> DELETE
        employees/{id} -> PUT
        